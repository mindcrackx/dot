#!/usr/bin/env bash

# Use nvim if available, fall back to vim, then vi
if command -v nvim &> /dev/null; then
  exec nvim "$@"
elif command -v vim &> /dev/null; then
  exec vim "$@"
else
  exec /usr/bin/vi "$@"
fi
